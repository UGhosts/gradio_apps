# 轴承异常判断分类应用

## 应用介绍

采用西交轴承数据集-轴承异常判断分类应用。用户可以选择测试文件，通过预训练的模型进行分析，并可视化识别结果。

### 主要功能

- 提供轴承异常判断分类应用识别程序使用
- 实时轴承振动原始数据
- 展示识别概率，多分类情况

## 模型介绍

该应用集成了轴承故障多分类模型，模型针对不同的特征提取方法和分类算法进行了优化。

### 模型特点

- 采用先进的深度学习架构，能够自动识别轴承振动数据集的分类情况
- 经过大量数据集的训练，具有较高的识别准确率
- 支持多种数据集的预测，可以定制化修改优化模型调参
- 针对不同场景的进行了优化，具有较强的泛化能力

### 使用方法

1. 在应用界面中选择所需的预训练模型
2. 点击预置的测试文件列表选择其中一个测试文件
3. 点击"处理"按钮进行分析
4. 查看生成的时序振动图和预测结果

### 模型选择说明

应用下拉菜单中显示的是模型目录名称，应用会自动加载选中模型进行预测分析。应用支持的模型列表，和使用数据集评估后的结果列表如下：

| 模型名称 | 数据集       | 准确率 | 模型存储大小（MB） |
|------|-----------|-----|------------|
| cnn  | XJTU-SY | 95+ | 131        |
数据集地址：
https://www.cnblogs.com/huxiaohu52/p/17406033.html

## 结果解释

模型分析完成后，应用会显示以下结果：

- 时域波形图：直观展示振动信号的时域特征
- 预测类别：模型预测多分类的类别
- 概率：模型对各个类别的计算概率


